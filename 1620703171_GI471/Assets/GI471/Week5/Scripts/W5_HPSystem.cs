using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class W5_HPSystem : MonoBehaviour
{
    //public Image hpBar;
    //public float maxHP = 100.0f;
    //public float currentHP;
    //public float hpRegenRate = 2.0f;

    //private W5_StatusSystem playerHP;
    private void Start()
    {
        //hpBar = GetComponent<Image>();

        //playerHP = FindObjectOfType<W5_StatusSystem>();
    }

    private void Update()
    {
        //currentHP = playerHP.playerHP;

        //if (currentHP < maxHP)
        //{
        //    playerHP.playerHP += hpRegenRate * Time.deltaTime;
        //}

        //hpBar.fillAmount = currentHP / maxHP;
    }

}

